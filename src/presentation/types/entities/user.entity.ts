export type UserEntity = {
    id: string
    isActive: boolean
    createdAt: string
    updatedAt: string
    name: string
    email: string
    phoneNumber: string
    picture: string
    role: string
}